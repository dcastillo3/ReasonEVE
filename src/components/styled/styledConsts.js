const body1 = 'body1';

const body2 = 'body2';

const caption = 'caption';

const label = 'label';

const h1 = 'h1';

const h2 = 'h2';

const h3 = 'h3';

const defaultVariant = 'primary';

const defaultElement = body1;

export {
    defaultVariant,
    defaultElement,
    body1,
    body2,
    caption,
    label,
    h1,
    h2,
    h3
};