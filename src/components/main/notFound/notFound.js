import React from 'react';
import { Box } from '../../styled';

function NotFound() {
    return (
        <Box m={[0, 8]}>
            404 not found
        </Box>
    );
};

export default NotFound;