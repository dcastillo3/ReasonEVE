import React, { Component } from 'react';

export class Packs extends Component {
  constructor(props) {
    super(props);
    this.state = {
    }
  }

  render() {

    return (
      <div id="packs" className="packs flex-column flex-center palette-three large-padding">
        Packs
      </div>
    )
  }
}

export default Packs;
